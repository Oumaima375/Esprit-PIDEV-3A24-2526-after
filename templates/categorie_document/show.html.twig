{% extends 'base.html.twig' %}

{% block title %}CategorieDocument{% endblock %}

{% block body %}
    <h1>CategorieDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCategorie</th>
                <td>{{ categorie_document.idCategorie }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie_document.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie_document.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_document_index') }}">back to list</a>

    <a href="{{ path('app_categorie_document_edit', {'idCategorie': categorie_document.idCategorie}) }}">edit</a>

    {{ include('categorie_document/_delete_form.html.twig') }}
{% endblock %}
