<form method="post" action="{{ path('app_document_crud_delete', {'idDocument': document.idDocument}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document.idDocument) }}">
    <button class="btn">Delete</button>
</form>
